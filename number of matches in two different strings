str1 = "hello"
str2 = "h3llo"

length = min(len(str1), len(str2))

match_count = 0

for i in range(length):
    if str1[i] == str2[i]:
        match_count += 1

print(f"Number of matches: {match_count}")
